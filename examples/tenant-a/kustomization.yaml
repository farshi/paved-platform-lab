resources:
  - namespace.yaml
  - rbac.yaml
  - resourcequota.yaml
  - limitrange.yaml
  - ../../services/demo-api/manifests/base

commonLabels:
  tenant: tenant-a

images:
  - name: ghcr.io/rfar/platform-guardrails-lab/demo-api
    newTag: 0.1.0

patches:
  - path: patch-env.yaml

